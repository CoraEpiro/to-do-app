const Selector = () => {
    return <select id="importanceSelector">
        <option value="true">True</option>
        <option value="false" selected>False</option>
    </select>
}

export default Selector